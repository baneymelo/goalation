/* eslint-disable no-unused-vars */
import { NavBar, Section} from "../components";

const Home = () => {
    return(
        <div>
            <NavBar />
            <Section />
        </div>
    )
}

export default Home;